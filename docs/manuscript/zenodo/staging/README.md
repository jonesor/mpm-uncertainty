docs/manuscript/zenodo/staging/
-------------------------------
Temporary staging area for Zenodo deposition candidates.

Each run of `scripts/prepare_zenodo_bundle.R` creates a timestamped folder here,
containing a full reproducibility bundle (code, data, outputs, manuscript files,
and manifest metadata).
