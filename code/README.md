code/
-----
Reusable R functions shared across analysis scripts.

Contents
--------
- `functions.R`   Core helper functions for matrix processing, sampling distributions, and utilities.
                Includes plot helpers (`theme_mpm()`, `mpm_colors()`).

Usage
-----
Scripts load this with `source("code/functions.R")`.
